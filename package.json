{
  "name": "settlement-with-wallet",
  "version": "1.0.0",
  "description": "Real-Time Blockchain Settlement for Indian Stock Markets",
  "main": "index.js",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}